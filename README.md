SAHRAB Advertising Bot V3Combined starter bot for python-telegram-bot v22Environment Variables:BOT_TOKEN=...ADMIN_ID=123456789CHANNEL_ID=@SAHRAB_Ads"""import osfrom telegram import Update, InlineKeyboardButton, InlineKeyboardMarkupfrom telegram.ext import Application, CommandHandler, CallbackQueryHandler, ContextTypesBOT_TOKEN = os.getenv("BOT_TOKEN","")ADMIN_ID = int(os.getenv("ADMIN_ID","0"))CHANNEL_ID = os.getenv("CHANNEL_ID","@SAHRAB_Ads")def menu():    return InlineKeyboardMarkup([        [InlineKeyboardButton("馃摙 SAKA TALLA", callback_data="advertise")],        [InlineKeyboardButton("馃挵 FARASHIN TALLA", callback_data="prices")],        [InlineKeyboardButton("馃挸 BIYA TALLA", callback_data="payment")],        [InlineKeyboardButton("馃摓 TUNTU苼I ADMIN", url="https://t.me/SAHRAB_Advertising_bot")],        [InlineKeyboardButton("馃摵 SHIGA CHANNEL", url="https://t.me/SAHRAB_Ads")]    ])async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):    await update.message.reply_text(        "Barka da zuwa SAHRAB Advertising Bot.",        reply_markup=menu()    )async def prices(update: Update, context: ContextTypes.DEFAULT_TYPE):    q=update.callback_query; await q.answer()    await q.edit_message_text(        "Farashin Talla\n\n鈧�500 - 24 Hours\n鈧�1,200 - 3 Days\n鈧�2,500 - 7 Days",        reply_markup=InlineKeyboardMarkup([[InlineKeyboardButton("猬咃笍 BAYA", callback_data="home")]])    )async def payment(update: Update, context: ContextTypes.DEFAULT_TYPE):    q=update.callback_query; await q.answer()    await q.edit_message_text(        "馃挸 BIYAN TALLA\n\n"        "Bank: Moniepoint MFB\n"        "Account Name: Abubakar Rabiu Abubakar\n"        "Account Number: 6674767017\n\n"        "Bayan ka biya, turo screenshot a wannan chat."    )async def advertise(update: Update, context: ContextTypes.DEFAULT_TYPE):    q=update.callback_query; await q.answer()    await q.edit_message_text(        "馃摙 Turo:\n1. Sunan kasuwanci\n2. Rubutun talla\n3. Hoto ko bidiyo\n4. Screenshot idan ka biya."    )async def home(update: Update, context: ContextTypes.DEFAULT_TYPE):    q=update.callback_query; await q.answer()    await q.edit_message_text("SAHRAB Advertising Bot", reply_markup=menu())async def admin(update: Update, context: ContextTypes.DEFAULT_TYPE):    if update.effective_user.id != ADMIN_ID:        return    await update.message.reply_text("Admin Panel - V3")app = Application.builder().token(BOT_TOKEN).build()app.add_handler(CommandHandler("start", start))app.add_handler(CommandHandler("admin", admin))app.add_handler(CallbackQueryHandler(prices, "^prices$"))app.add_handler(CallbackQueryHandler(payment, "^payment$"))app.add_handler(CallbackQueryHandler(advertise, "^advertise$"))app.add_handler(CallbackQueryHandler(home, "^home$"))if __name__ == "__main__":    app.run_polling()
